{
  "name": "firebackend",
  "version": "1.0.0",
  "description": "Fire app backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "migrate": "npx knex migrate:latest",
    "rollback": "npx knex migrate:rollback",
    "seed": "npx knex seed:run "
  },
  "author": "Suraj",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "knex": "^1.0.7"
  }
}
